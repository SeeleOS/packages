diff --git libxcb-clean/configure libxcb-workdir/configure
index 33f5d89..3fd3b66 100755
--- libxcb-clean/configure
+++ libxcb-workdir/configure
@@ -21028,7 +21028,7 @@ fi
 NEEDED="xau >= 0.99.2"
 case $host_os in
 linux*|darwin*|solaris*|dragonfly*|freebsd*|netbsd*) ;;
-     *) NEEDED="$NEEDED pthread-stubs" ;;
+     *) NEEDED="$NEEDED" ;;
 esac
 
 pkg_failed=no
diff --git libxcb-clean/doc/Makefile.in libxcb-workdir/doc/Makefile.in
index 8448668..2fee3ef 100644
--- libxcb-clean/doc/Makefile.in
+++ libxcb-workdir/doc/Makefile.in
@@ -518,8 +518,8 @@ install-data-local:
 	  $(mkinstalldirs) '$(DESTDIR)$(htmldir)'; \
 	fi
 	@for d in $(docdirs); do \
-	  echo "cp -pR $$d '$(DESTDIR)$(htmldir)/'"; \
-	  cp -pR $$d '$(DESTDIR)$(htmldir)/'; \
+	  echo "cp -R $$d '$(DESTDIR)$(htmldir)/'"; \
+	  cp -R $$d '$(DESTDIR)$(htmldir)/'; \
 	done
 
 uninstall-local:

--- a/src/xcb_util.c
+++ b/src/xcb_util.c
@@ -483,9 +483,11 @@
         setsockopt(fd, SOL_SOCKET, SO_SNDBUF, &val, sizeof(int));
     }
     if(connect(fd, (struct sockaddr *) &addr, sizeof(addr)) == -1) {
+        fprintf(stderr, "xcb: unix connect failed path=%s errno=%d\n", file, errno);
         close(fd);
         return -1;
     }
+    fprintf(stderr, "xcb: unix connect ok path=%s fd=%d\n", file, fd);
     return fd;
 }
 #endif /* !_WIN32 */
